apiVersion: v1
kind: Service
metadata:
  name: ttdfs
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ttdfs"
    port: 8080
  selector:
    app: ttdfs
